class Childrens extends Price {
  fun amount(daysRented: int): float {
    var thisAmount = 1.5;
    if (daysRented > 3) {
      thisAmount += (daysRented - 3) * 1.5;
    }
    return thisAmount;
  }
}
